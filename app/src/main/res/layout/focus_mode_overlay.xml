<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/focus_mode_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    android:clickable="true"
    android:focusable="true">

    <!-- Message list - bottom padding is set dynamically via WindowInsets -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/focus_recycler"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipToPadding="false"
        android:overScrollMode="never"
        android:paddingTop="48dp"
        android:paddingBottom="64dp" />

    <!-- Presence dot + draft text anchored just above keyboard -->
    <LinearLayout
        android:id="@+id/focus_bottom_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:paddingBottom="20dp"
        android:paddingHorizontal="24dp">

        <TextView
            android:id="@+id/focus_draft_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="14dp"
            android:alpha="0.5"
            android:fontFamily="sans-serif-light"
            android:gravity="center"
            android:textColor="#FFFFFFFF"
            android:textSize="16sp"
            android:visibility="gone" />

        <View
            android:id="@+id/focus_presence_dot"
            android:layout_width="7dp"
            android:layout_height="7dp"
            android:layout_gravity="center"
            android:background="@drawable/focus_mode_dot"
            android:visibility="invisible" />

    </LinearLayout>

</FrameLayout>
